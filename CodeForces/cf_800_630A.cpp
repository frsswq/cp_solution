#include <bits/stdc++.h>

using ll = long long;
using ull = unsigned long long;
using ld = long double;

// lol, the power of 5 will always result with last two number of 25

int main () {
    using namespace std;
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    ll n;
    cin >> n;

    cout << 25 << endl;

    return 0;
}