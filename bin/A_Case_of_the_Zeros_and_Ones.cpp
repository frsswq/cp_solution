#include <bits/stdc++.h>

using ll = long long;
using ull = unsigned long long;
using ld = long double;

int main () {
    using namespace std;
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;

    // not the right approach
    // time out at large n

    // string s;
    // cin >> s;

    // string t = "10";
    // string u = "01";

    // int len_t = t.size();

    // while(s.find(t) != -1) {
    //     s.erase(s.find(t), len_t);
    // }

    // while(s.find(u) != -1) {
    //     s.erase(s.find(u), len_t);
    // }

    // cout << s.size() << endl;

    return 0;
}